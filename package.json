{
  "name": "delen",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "server": "node server/app.js",
    "client": "npm start --prefix client",
    "delen": "concurrently \"npm run server\" \"npm run client\"",
    "postinstall": "cd client && npm install --unsafe-perm && cd ../server && npm install --unsafe-perm"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0"
  }
}
